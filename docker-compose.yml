version: "3.8"
services:
  web:
    build: .
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_URL=https://econgame.dousek.org
    env_file:
      - .env
    ports:
      - "8007:3000"
    