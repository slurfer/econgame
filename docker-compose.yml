version: "3.8"
services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "8007:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_URL=http://econgame.dousek.org
    